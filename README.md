# Modux

Projeto de IHC/UX

# Uso

Crie um arquivo .env com a chave da API:
```.env
VITE_GROQ_API_KEY=<chave da API>
```

Instale as dependências:

```bash
user % npm install 
```

Rode como desenvolvedor:

```bash
user % npm run dev
```

Esse comando iniciará um servidor local na porta 5173.